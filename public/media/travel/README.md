# Travel photos

Drop real images at paths referenced in `src/content/travel.ts`:

```
public/media/travel/{tripId}/{photoId}.jpg
```

Until files exist, the UI shows CSS placeholders (`placeholder: true` on each photo).
