name: victor-os

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: victor-os
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
